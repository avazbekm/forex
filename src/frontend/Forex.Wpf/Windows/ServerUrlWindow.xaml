<Window
    x:Class="Forex.Wpf.Windows.ServerUrlWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="Server URL"
    Width="430"
    Height="230"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize">
    <Border Padding="18" Background="{DynamicResource PrimaryBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                FontSize="18"
                FontWeight="SemiBold"
                Text="Server URL sozlash" />

            <TextBlock
                Grid.Row="1"
                Margin="0,12,0,4"
                Foreground="{DynamicResource InkMuted}"
                Text="API manzili" />

            <TextBox
                x:Name="tbUrl"
                Grid.Row="2"
                Height="34"
                Padding="8,4"
                VerticalContentAlignment="Center" />

            <TextBlock
                x:Name="tbStatus"
                Grid.Row="3"
                Margin="0,10,0,0"
                TextWrapping="Wrap" />

            <StackPanel
                Grid.Row="4"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Width="95"
                    Margin="0,0,8,0"
                    Click="BtnTest_Click"
                    Content="Tekshirish" />
                <Button
                    Width="85"
                    Margin="0,0,8,0"
                    md:ButtonAssist.CornerRadius="4"
                    Background="{StaticResource SuccessButtonBackground}"
                    BorderBrush="{StaticResource SuccessButtonBorder}"
                    Click="BtnSave_Click"
                    Content="Saqlash"
                    Foreground="{StaticResource SuccessButtonForeground}" />
                <Button
                    Width="75"
                    Click="BtnCancel_Click"
                    Content="Bekor" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
