<UserControl
    x:Class="Forex.Wpf.Pages.Settings.Controls.ServerSettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="20"
            FontWeight="SemiBold"
            Text="Server" />

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                Height="36"
                Padding="10,4"
                md:HintAssist.Hint="API URL"
                Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" />

            <Button
                Grid.Column="1"
                Width="110"
                Margin="10,0,0,0"
                Command="{Binding TestCommand}"
                Content="Tekshirish" />

            <Button
                Grid.Column="2"
                Width="95"
                Margin="10,0,0,0"
                Background="{StaticResource SuccessButtonBackground}"
                BorderBrush="{StaticResource SuccessButtonBorder}"
                Command="{Binding SaveCommand}"
                Content="Saqlash"
                Foreground="{StaticResource SuccessButtonForeground}" />
        </Grid>

        <TextBlock
            Grid.Row="2"
            Margin="0,12,0,0"
            Foreground="#666666"
            Text="{Binding StatusText}"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>
