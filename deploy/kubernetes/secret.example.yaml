# NAMUNA. Nusxalang -> secret.yaml, qiymatlarni to'ldiring (secret.yaml git'ga kirmaydi).
#   cp secret.example.yaml secret.yaml && nano secret.yaml
# stringData oddiy matn qabul qiladi (base64 shart emas).
apiVersion: v1
kind: Secret
metadata:
  name: forex-secrets
  namespace: forex
type: Opaque
stringData:
  POSTGRES_PASSWORD: "kuchli-postgres-parol"
  MINIO_ROOT_PASSWORD: "kuchli-minio-parol"
  PGADMIN_PASSWORD: "kuchli-pgadmin-parol"
  JWT_SECRET_KEY: "uzun-tasodifiy-jwt-kalit-kamida-32-belgi"
  # Backup (full/zaxira) uchun:
  BACKUP_PASSWORD: "kuchli-backup-shifr-parol"
  # cert-manager (Let's Encrypt Cloudflare DNS-01) uchun:
  CF_DNS_API_TOKEN: "cloudflare-token"
